version: '3.8'

services:
  ai-governance-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: always
    container_name: ai_governance_dashboard
